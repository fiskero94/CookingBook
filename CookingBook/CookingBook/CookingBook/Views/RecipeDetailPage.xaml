<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="CookingBook.Views.RecipeDetailPage"
             Title="{Binding Recipe.Title}">

    <ScrollView>
        <StackLayout>
            <Image Source="{Binding Recipe.Image}" Aspect="AspectFill" HeightRequest="150"/>
            <StackLayout Padding="16, 8, 16, 8">
                <Label Text="{Binding Recipe.Title}" FontSize="28" TextColor="Black"/>
                <Label Text="{Binding ServingsReadyInMinutesText}"/>
                <Label Text="{Binding CreditsText}" FontSize="11" Margin="0, 0, 0, 16"/>
                <Label Text="{Binding SummaryText}" TextColor="#333333"/>
                <!--<Label Text="Ingredients" TextColor="Black" Margin="0, 16, 0, 0"/>-->
                <!--<Label Text="{Binding IngredientsText}" TextColor="#333333" Margin="0, 0, 0, 16"/>-->
                <Label Text="{Binding Recipe.Instructions}" TextColor="#333333" />
            </StackLayout>
        </StackLayout>
    </ScrollView>

</ContentPage>